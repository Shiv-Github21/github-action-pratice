name: Schedule-Trigger

on:
  schedule:
    # - cron: 0 0 * * *
      - cron: 51 13 * * *

jobs:
  run-workflow-at-midnight-ist:
    runs-on: ubuntu-latest

    steps:
      - name: clonning-code
        uses: action/checkout@v7

      - name: Workflow running
        run: echo"the workflow is running at every-midnight-IST-Zone"
